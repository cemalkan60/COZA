# COZA backend — standalone dependencies (no platform-specific packages).
# Only the libraries the app actually imports are listed; pip resolves transitives.

# Web framework / server
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0

# MongoDB
motor==3.3.1
pymongo==4.6.3
dnspython==2.8.0

# Data models / validation
pydantic==2.13.4
email-validator==2.3.0

# Config
python-dotenv==1.2.2

# Auth
PyJWT==2.13.0
bcrypt==4.1.3

# Scheduler (daily/weekly scrape job)
APScheduler==3.11.3
tzlocal==5.4.4
tzdata==2026.3

# HTTP client (scraper -> Zara ES via proxy)
requests==2.34.2

# COZA Fashion (fashion-press.net / nowfashion.com / firstview.com scrape,
# JA->TR translation, and perceptual-hash photo de-duplication across sources)
beautifulsoup4==4.12.3
deep-translator==1.11.4
Pillow>=11.3
ImageHash>=4.3

# COZA Fashion — R2 image cache (S3-compatible client; optional, only used
# once R2_* env vars are set — see image_store.py)
boto3>=1.34

# Testing
pytest==9.1.1
httpx==0.28.1
